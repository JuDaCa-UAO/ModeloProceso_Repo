/**
 * CONTENT — Diálogos de Laia · Cierre (La espiral continúa)
 *
 * PLACEHOLDER INERTE. El cierre aún no está implementado. Reserva el espacio
 * estructural y la convención de nombres; NO se importa en ningún lado todavía.
 *
 * Nota: el cierre tampoco está registrado en `STAGE_META` ni en
 * `StaticStageContentRepository`, por lo que no es navegable hasta que se
 * implemente. Consumir estos pasos desde `presentation/stages/closing/`.
 */

import type { CharacterDialogStep } from "@/components/character-step-dialog/CharacterStepDialog";

export const CLOSING_LAIA_STEPS: CharacterDialogStep[] = [];
