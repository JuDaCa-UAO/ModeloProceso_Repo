/**
 * CONTENT — Diálogos de Laia · Etapa 2 (Descubre nuevas posibilidades)
 *
 * PLACEHOLDER INERTE. La Etapa 2 aún no está implementada. Este archivo reserva
 * el espacio estructural y la convención de nombres; NO se importa en ningún lado
 * todavía, por lo que no afecta el flujo actual.
 *
 * Para implementar la etapa: poblar estos arrays con los pasos de diálogo
 * (mismo tipo `CharacterDialogStep` que las etapas 0 y 1) y consumirlos desde la
 * vista de la etapa en `presentation/stages/stage-2-discover/`.
 */

import type { CharacterDialogStep } from "@/components/character-step-dialog/CharacterStepDialog";

export const STAGE2_LAIA_STEPS: CharacterDialogStep[] = [];
